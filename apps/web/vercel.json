{
  "framework": "nextjs",
  "installCommand": "npm install --prefer-offline --no-audit --fund=false",
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "regions": ["iad1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 15
    }
  }
}
